(()=>{const all=[...document.querySelectorAll('.perm-card input[type=checkbox]')],count=document.getElementById('permCount');const refresh=()=>{if(count)count.textContent=`${all.filter(x=>x.checked).length} / ${all.length}`};document.querySelectorAll('.group-toggle').forEach(t=>t.addEventListener('change',()=>{document.querySelectorAll(`[data-perm-group="${t.dataset.group}"]`).forEach(x=>{if(!x.disabled)x.checked=t.checked});refresh()}));all.forEach(x=>x.addEventListener('change',refresh));refresh()})();
